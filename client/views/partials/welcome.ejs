<% if(locals.user) { 
    if(user.member) {
        %>
        <p class="welcome-text">Welcome, <%= (!!locals.user.firstName)?user.firstName:'' %>! <span>Thank you for being a member!</span><br />
        <span class="signout-link"><a href="/signOut">[ SIGN OUT ]</a></span></p>
        <% 
    } 
    else {
        %>
        <p class="welcome-text">Welcome, <%= (!!locals.user.firstName)?user.firstName:'' %>!<span> - You have <%- user.availableAccessTokens %> tokens</span><br />
        <span class="signout-link"><a href="/signOut">[ SIGN OUT ]</a></span></p>
        <% 
    }    
} 
%>