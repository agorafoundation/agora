<div class="modal" tabindex="-1" id="delete-workspace-modal" aria-labelledby="delete-workspace-modal-label" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title"> Workspace <div id="workspace-title"></div></h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body">
          <p>Are you sure you want to delete this workspace?</p>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" onclick="closeDeleteModal()">No</button>
          <button type="button" class="btn btn-primary" onclick="sendDeleteToBackend(parseInt(document.getElementById('workspace-title').innerHTML),10)">Yes</button>
        </div>
      </div>
    </div>
  </div>

  <script>
    const closeDeleteModal = () => {
      // closes modal
      $('#delete-workspace-modal').modal('hide');

      // removes darker backdrop after closing modal
      $('body').removeClass('modal-open');
      $('.modal-backdrop').remove();
    }

    const sendDeleteToBackend = async (id) => {
        await bankendDelete(id);
        closeDeleteModal();
    }


  </script>