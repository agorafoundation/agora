<!-- Defuct - URBG - 20221213 -->

<% if(locals.authUser) { 
    if(locals.authUser.member) {
        %>
        <p class="welcome-text">Welcome, <%= (!!locals.authUser.firstName)?authUser.firstName:'' %>! <span>Thank you for being a member!</span><br />
        <span class="signout-link"><a href="/signOut">[ SIGN OUT ]</a></span></p>
        <% 
    } 
    else {
        %>
        <p class="welcome-text">Welcome, <%= (!!locals.authUser.firstName)?authUser.firstName:'' %>!<br />
        <span class="signout-link"><a href="/signOut">[ SIGN OUT ]</a></span></p>
        <% 
    }    
} 
%>